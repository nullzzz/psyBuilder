2019.6.26 李扬
修改了UI，稍微顺眼了一点，配色太难了。
可以复制粘贴（如果目标区域小于源区域，自动增加目标区域行和列都是，对于列而言涉及到变量名，自动0,1,2,3累加就好了，如untitlevar1,
可以选中复制出来，方便大家copy到excel或其他软件中中修改；
这个改一下，修改默认值只改变那些为空的地方，现有的无论是自动旧默认值填充来的还是新输入的都不变；
自动拖拽的地方，最好同excel一样，选中某些框之后，鼠标放到框的右下角变成+，然后就可以拖拽了


2019.6.12 于鹏
完善导入机制
    加入导入前清空功能
初步尝试控制台输出
    提供控制台输出接口Func.log(text, error, timer)
更新部分文档注释

2019.6.11 李扬
将widget类型统一用Info.xxx表示
将图片路径统一使用Func.getImage(image_name)表示